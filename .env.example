# Use a Groq Free plan account; never commit real keys.
# Do not upgrade to Developer if you want zero API charges.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
# This model runs on Groq, not the paid OpenAI API. OPENAI_API_KEY is ignored.
# Local development: node --env-file=.env server/index.js
PORT=8080
DB_PATH=data/intellipath.db
